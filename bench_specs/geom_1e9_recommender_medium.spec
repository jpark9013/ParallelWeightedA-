# Real-world analogy: embedding similarity graph (recommender / IR).
# Medium kNN: each node links to ~64 nearest neighbors among 512 sampled candidates.
type=geom_hash_knn
n=1000000000
seed=4242
k=64
candidates=512
